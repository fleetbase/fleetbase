<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd" bootstrap="vendor/autoload.php" colors="true" cacheDirectory=".phpunit.cache">
  <testsuites>
    <testsuite name="Unit">
      <directory suffix="Test.php">./tests/Unit</directory>
    </testsuite>
    <testsuite name="Feature">
      <directory suffix="Test.php">./tests/Feature</directory>
    </testsuite>
  </testsuites>
  <php>
    <server name="APP_ENV" value="testing"/>
    <server name="BCRYPT_ROUNDS" value="4"/>
    <server name="CACHE_DRIVER" value="array"/>
    <!-- <server name="DB_CONNECTION" value="sqlite"/> -->
    <!-- <server name="DB_DATABASE" value=":memory:"/> -->
    <server name="MAIL_MAILER" value="array"/>
    <server name="QUEUE_CONNECTION" value="sync"/>
    <server name="SESSION_DRIVER" value="array"/>
    <server name="TELESCOPE_ENABLED" value="false"/>
  </php>
  <source>
    <include>
      <directory suffix=".php">./app</directory>
    </include>
    <exclude>
      <!--
        Dead Laravel skeleton file. App\Providers\BroadcastServiceProvider is
        commented out of config/app.php's provider list, and its boot() requires
        routes/channels.php, which this app does not have — booting it would
        fatal. Broadcasting is registered by Illuminate's own provider instead.
        Excluded rather than tested for the same reason console/app/resolver.js
        is excluded in console/config/coverage.js: nothing imports it, and it
        would throw if it were wired up. Delete the file (and this exclude) or
        wire it up properly — do not add a contrived test for it.
      -->
      <file>./app/Providers/BroadcastServiceProvider.php</file>
    </exclude>
  </source>
</phpunit>
