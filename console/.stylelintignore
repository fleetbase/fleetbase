# unconventional files
/blueprints/*/files/

# compiled output
/dist/

# addons
/.node_modules.ember-try/

# coverage report output (generated by ember-cli-code-coverage, gitignored).
# .eslintignore already excludes it; stylelint needs telling separately, otherwise
# `pnpm lint` passes or fails depending on whether a coverage run happened first.
/coverage/
