# unconventional js
/blueprints/*/files/

# compiled output
/dist/

# misc
/coverage/
!.*
.*/

# ember-try
/.node_modules.ember-try/

# generated by lib/fleetbase-extensions-generator at build time — linting these is
# pointless because the generator rewrites them on every build, and whether they exist
# at lint time depends on whether a build has run (they are gitignored)
/app/router.js
/app/extensions/
