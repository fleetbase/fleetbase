<div class="date-picker-container relative">
    <Input @value={{@value}} placeholder={{@placeholder}} autocomplete="off" class="fleetbase-date-picker form-input {{@class}}" ...attributes {{did-insert this.setupComponent}} />

    {{#if this.hasValue}}
        <span class="ember-power-select-clear-btn absolute" role="button" {{on "click" this.clear}}>×</span>
    {{/if}}
</div>
