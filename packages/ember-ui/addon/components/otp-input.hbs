<div class="otp-input-container">
    <Input
        @value={{this.value}}
        @type="tel"
        class="form-input form-input-lg otp-input"
        autocomplete="off"
        {{did-insert this.setup}}
        placeholder={{this.placeholder}}
        {{on "input" this.validate}}
        ...attributes
    />
</div>