<EmberWormhole @to="sidebar-menu-items">
    <Layout::FleetOpsSidebar @controller={{this}} @onClickCreateOrder={{this.createOrder}} @onClickSettings={{this.toggleSettings}} />
</EmberWormhole>

<Layout::Section::Container>
    {{outlet}}
</Layout::Section::Container>

<!-- comment out for help tour -->
{{!-- <SettingsWindow @isOpen={{false}} @onLoad={{this.setSettingsContext}} @onPressCancel={{this.closeSettings}} /> --}}
<ContextPanel />