<Modal::Default @modalIsOpened={{@modalIsOpened}} @options={{@options}} @confirm={{@onConfirm}} @decline={{@onDecline}}>
    <div class="modal-body-container">
        <div class="grid grid-cols-1 gap-2 text-xs dark:text-gray-100">
            <InputGroup @name={{t "fleet-ops.component.modals.place-assign-vendor.select-vendor"}} @value={{@options.place.vendor_uuid}}>
                <ModelSelect
                    @modelName="vendor"
                    @selectedModel={{@options.place.vendor_uuid}}
                    @placeholder={{t "fleet-ops.component.modals.place-assign-vendor.select-vendor"}}
                    @triggerClass="form-select form-input"
                    @infiniteScroll={{false}}
                    @renderInPlace={{true}}
                    @onChange={{fn (mut @options.place.vendor_uuid)}}
                    as |model|
                >
                    {{model.name}}
                </ModelSelect>
            </InputGroup>
        </div>
    </div>
    {{yield}}
</Modal::Default>